import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tarea botones y rutas primera clase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
